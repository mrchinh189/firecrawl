# ===== Thu thập (ưu tiên FIRECRAWL) =====
FIRECRAWL_KEY=             # firecrawl.dev — nguồn cào CHÍNH (JS + chống chặn)
FETCH_PREFER=firecrawl     # firecrawl | http  (thứ tự fallback)

# ===== Nguồn FREE (API/XML) =====
EIA_KEY=                   # eia.gov/opendata — Brent
COMTRADE_KEY=              # comtradeplus.un.org — unit value NK
FRED_API_KEY=             # fred.stlouisfed.org — backfill PPI/palm oil/brent dài

# ===== Claude (Anthropic) — narrative/commentary/extract/news =====
ANTHROPIC_API_KEY=         # console.anthropic.com (thiếu -> fallback template/thống kê)

# ===== Lưu trữ =====
SUPABASE_DB_URL=           # Supabase → Settings → Database → Connection string (thiếu -> dùng fixtures)

# ===== Telegram (đẩy báo cáo) =====
TELEGRAM_BOT_TOKEN=        # @BotFather
TELEGRAM_CHAT_ID=          # @userinfobot
DASHBOARD_URL=             # link dashboard Vercel (tùy chọn, gắn vào tin Telegram)

# ===== Lark (tùy chọn) =====
LARK_WEBHOOK=

# ===== repo_dispatch cho webhook Telegram 2 chiều (Phase 3) =====
GH_DISPATCH_PAT=
GH_OWNER=
GH_REPO=
TELEGRAM_WEBHOOK_SECRET=
